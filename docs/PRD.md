# ICONS — 제품 요구사항 정의서 (PRD)

> 상태: Draft · 최종 수정 2026-08-13 · 작성 근거: 현재 코드베이스(프로토타입) + 설계 인터뷰
> 이 문서는 **무엇을 만들 것인가**를 정의한다. **어떻게 만들 것인가**는 [`ARCHITECTURE.md`](./ARCHITECTURE.md) 참조.

---

## 1. 개요

**ICONS**는 서브컬처 팬덤을 위한 **슈퍼앱**이다. 공식 라이선스 굿즈 커머스를 척추로, 수집형 디지털 카드(무료 리워드), 팝업/티케팅, 팬 커뮤니티, IP 허브를 하나의 "Holographic Midnight" 경험으로 묶는다.

현재 리포(`/Users/sangwopark19/icons/icons-ip`)는 Claude Design 핸드오프를 옮긴 **시각적 프로토타입**에서 출발했다. App Router 화면에 Supabase 공개 카탈로그, 이메일/PW Auth, 온보딩, IP 팔로우, 커뮤니티, 검색, admin 경로가 연결되어 있다. 굿즈 실장바구니와 체크아웃·토스 승인/웹훅, 본인 주문 내역·청약철회 요청, 관리자 주문·배송·환불·실재고·티켓 회차·회원 제재·카탈로그 아트워크 업로드·보관/복원·홈 큐레이션 콘솔, 공개 회차 선택·티켓 예매 결제·웹훅 QR 발급, 내 티켓·예매 취소/환불, staff 현장 검표까지 연결됐다. 이 PRD는 프로토타입을 **실제 출시 제품**으로 전환하기 위한 요구사항을 정의한다.

### 1.1 제품 비전

> "흩어진 서브컬처 팬덤의 소비·수집·소통을 한 곳에서 — 공식 라이선스의 신뢰 위에서."

### 1.2 목표 (v1)

- 공식 라이선스 굿즈를 안전하게 **구매**할 수 있다 (장바구니 → 결제사 승인 → 주문).
- IP별 **수집형 디지털 카드**를 무료 카드팩·참여형 게임으로 받고 **바인더**에 모은다.
- **팝업** 티켓을 예매하고 전자티켓(QR)으로 입장한다.
- 관심 IP를 **팔로우**하고 팬들과 **커뮤니티**에서 소통한다.

### 1.3 비목표 (v1에서 하지 않는 것)

- **C2C 마켓**(중고 거래)·**교환**(직거래·경매): UI는 유지하되 거래·정산·에스크로는 구현하지 않는다 → **v2**.
- **유료 멤버십/구독**(IP 팬덤 유료 티어) → v2.
- 실시간 채팅/라이브 피드 → 검토 후 차기.
- 글로벌(다국어·해외결제) 본격 대응 → 차기.
- 범용 온라인 팝업 운영 레이어(popups 운영 단위·미션·등급·래플·브랜드 리포트)와 Expo webview 앱 호스트는 현 로드맵에서 만들지 않는다. 현재 웹 티케팅·참여형 게임은 이 비목표와 별개로 유지한다.

---

## 2. 대상 사용자

| 페르소나 | 설명 | 핵심 니즈 |
|---|---|---|
| **수집러** | 포토카드·홀로 카드 컬렉션에 진심인 팬 | 카드팩 개봉 경험, 보유 현황(바인더), 한정 드롭 알림 |
| **굿즈 구매자** | 공식 라이선스 굿즈를 정품으로 사고 싶은 팬 | 신뢰(정품)·재고·빠른 결제 |
| **이벤트 참여자** | 팝업에 가는 팬 | 회차/잔여 확인, 간편 예매, 전자티켓 |
| **커뮤니티 유저** | 인증샷·후기·교환글로 소통하는 팬 | 작성·반응·신고/차단 안전장치 |
| **운영자(내부)** | IP·상품·카드풀·이벤트·신고를 관리 | 백오피스(`/admin`)에서 카탈로그·확률 공시·모더레이션 |

브라우징은 **로그인 없이 공개**. 액션(구매·카드팩 개봉·게임 플레이·예매·작성·팔로우) 시점에 로그인을 요구한다.

---

## 3. 현재 상태와 갭

| 영역 | 프로토타입 현황 | v1 목표 갭 |
|---|---|---|
| 데이터 | Supabase 환경변수가 있으면 공개 카탈로그와 검색, 커뮤니티 피드를 DB에서 읽고, 없으면 `lib/data.ts` mock으로 fallback. P0~P3 migration과 seed가 존재 | 결제/카드 리워드/티켓 도메인별 쓰기 흐름 연결 |
| 인증 | 이메일/PW 가입·로그인, 확인 메일 콜백·재전송, 비밀번호 재설정, 온보딩 게이트. Google/Apple/Kakao OAuth 코드와 production provider·필수 이메일 claim 설정 완료. 현재 생년월일은 자가신고 값이며 연령보증 증거가 아니다 | production 배포 후 controlled smoke, 계정 삭제·동의 철회, 14+ 공통 연령보증 계약·기존 계정 처리 [#188](https://github.com/icons-hq/icons-ip/issues/188) |
| IP 팔로우 | 온보딩 추천 IP 저장, IP 상세 팔로우/언팔로우·알림 설정, 홈 커뮤니티 우선순위와 내 팬덤 피드가 연결 | 완료 |
| 장바구니·주문 | 비로그인 localStorage → 로그인 `cart_items` 병합, 수량·재고 검증, 본인 주문 내역·상세·청약철회 상태, 관리자 주문 검색·배송 전이·환불 정합화·실재고 수동 조정 | 완료 |
| 결제 | 토스 결제위젯, 승인 API, 웹훅 확정, 만료 정리 연결. production은 기본 라이브 키만 허용하되, 승인된 임시 사람 검토는 Preview/로컬에서 검증한 `test_gck_…`/`test_gsk_…`, 승인 키 쌍 지문, 서버 전용 override가 모두 맞을 때 활성 `staff`/`admin` 계정에만 허용한다. 그 밖의 계정은 주문·예매 생성과 승인·웹훅 확정을 차단하며 우회 승인 건은 자동 취소한다. 테스트 결제는 실제 결제수단을 출금하지 않으며 그 밖의 경우 fail closed | 검토 승인 뒤 override 제거, 라이브 위젯 키·지문 복원, 라이브 상점 계약·키·웹훅 등록 검증 |
| 카드 리워드 | 뽑기권(카드팩) 인벤토리·개봉과 바인더, 주문 결제 시 조건에 맞는 복수 정책의 누적 발급, 대상 IP·선택 same-IP 굿즈·독립 카드풀을 운영하는 관리자 정책 콘솔, 카드 보상형 참여형 게임·운영 콘솔과 서버 결정 `play_game` 연결 | legacy `goods` variant는 읽기 전용이며 범용 래플 운영은 현 로드맵 밖 |
| 팝업 | 이벤트 목록·상세, audited 회차·현장 검표 콘솔, 멱등 예약·결제·웹훅 QR 발급, 본인 티켓 QR·예매 전체 취소/환불 | production 실제 결제 검증 |
| 커뮤니티 | 공개 전체/팔로우 IP 기반 내 팬덤 피드와 IP 상세 preview 읽기, 최근 7일 트렌딩, 포스트·댓글·좋아요·작성자 삭제·신고·차단·운영자 숨김 코드가 연결. 모든 post/comment 생성·수정과 community 이미지 upload는 DB·Storage gate에서 기본 OFF | 실제 수령인·대체 reviewer·통지·이의·복원·권리신고 경로와 rehearsal 뒤 별도 migration으로 활성화. 공개 읽기·신고·본인 삭제·권리행사는 유지 |
| 검색 | Postgres `search_public_content` RPC로 IP·굿즈·카드·visible 포스트·태그를 그룹 검색 | 최근 검색어·인기 검색어 persistence |
| 아트워크 | staff 전용 Supabase Storage 업로드·미리보기·교체 경로. 실제 라이선스 이미지는 운영자가 점진 투입 | 실제 콘텐츠 투입 |
| 운영 | staff/admin 게이트, 카탈로그 CRUD·아트워크 업로드·감사되는 보관/복원, 카드풀 운영 기간·등급별 발급 확률·카드 풀 바인딩, audited 뽑기권 발급 정책·참여형 게임 운영, 커뮤니티 신고 처리, 주문·배송·청약철회/환불·실재고·티켓 회차·현장 검표, 마스킹 회원 검색·명시적 상세·감사되는 계정 정지/해제, 홈 히어로·특집 IP·공지 배너 큐레이션 콘솔 | 완료 |

---

## 4. 범위

### 4.1 v1 포함

1. 인증 & 온보딩
2. IP 허브 (브라우징 + 무료 팔로우)
3. 굿즈 커머스 (장바구니 · 결제 · 주문) — **척추**
4. 무료 카드 리워드(카드팩·참여형 게임) + 바인더
5. 팝업/티케팅 (실제 예매 · 전자티켓)
6. 커뮤니티 (작성형)
7. 검색
8. 운영 백오피스(`/admin`)

### 4.2 v2 이후

- C2C 마켓 · 교환(직거래·경매) — 에스크로·정산·분쟁·통신판매중개
- 유료 멤버십/구독
- 실시간 기능, 글로벌 확장

### 4.3 별도 승인 트랙

- **19+ 꽝 없는 유한 실물 쿠지**는 기존 카드 리워드·참여형 게임·legacy 래플의 후속 기능이 아니다. 별도 `prize_sale` 원장과 서버 배정 계약은 [#212](https://github.com/icons-hq/icons-ip/issues/212), 공개 확률·last-one·결과 영수증·운영은 [#213](https://github.com/icons-hq/icons-ip/issues/213)이 추적한다.
- 이 트랙은 Korpay 결제, NICE `adult_19` 증거([#209](https://github.com/icons-hq/icons-ip/issues/209)·[#210](https://github.com/icons-hq/icons-ip/issues/210)), 법률·IP·재고·환불 evidence가 모두 맞기 전에는 활성화하지 않는다. 현재 v1 코드가 이 기능을 제공한다고 해석하지 않는다.

---

## 5. 기능 요구사항

표기: **P0/P1/P2/P3**는 [§9 출시 단계](#9-출시-단계)의 빌드 순서. `M`=필수(MUST), `S`=권장(SHOULD).

### 5.1 인증 & 온보딩 (P0)

- `M` 이메일/비밀번호 가입·로그인.
- `M` 이메일 확인 메일 콜백과 재전송. 같은 브라우저에서 같은 이메일로 반복 가입을 시도하면 최신 확인 메일을 재발송하되, 과도한 반복은 10분 window로 제한한다.
- `M` 비밀번호 재설정: 계정 존재 여부를 노출하지 않는 메일 요청 → PKCE callback → 새 비밀번호 저장 → 전역 로그아웃. 같은 브라우저의 정규화 이메일별 요청은 총 3회/10분으로 제한하고 provider rate limit을 실제 상한으로 둔다.
- `M` 소셜 로그인: **Google · Apple · Kakao**. 세 버튼은 Supabase 관리형 OAuth에 연결됐고 production provider도 활성화됐다. Google은 production 공개, Apple은 웹 App ID·Services ID·키, Kakao는 비즈 앱·필수 `account_email` 동의 구성이 완료됐다. production 배포 뒤 신규·기존 사용자 controlled smoke가 남아 있다.
- `M` 소셜 로그인이어도 **온보딩에서 이메일·닉네임·생년월일(자가신고)·약관/마케팅 동의를 수집**하여 프로필을 완성한다.
- `M` 공개 페이지는 비로그인 접근 가능. 보호 액션은 로그인 게이트.
- `M` v1 가입 기준은 **만 14세 이상**이다. 다만 현재 `profiles.birth_date` 자가신고만으로는 이 기준을 충족했다고 간주하지 않는다. 서버 강제 지점·만 나이 경계·기존 계정 dry-run·법정 문서·`minimum_age_14` 증거 계약은 [#188](https://github.com/icons-hq/icons-ip/issues/188)의 승인과 구현 증거가 갖춰질 때까지 미완료다. 이 문서 변경만으로 production gate를 열지 않는다.
- `M` 19+ 상품 접근은 NICE 기반 `adult_19` 증거를 별도 purpose로 사용한다([#209](https://github.com/icons-hq/icons-ip/issues/209)·[#210](https://github.com/icons-hq/icons-ip/issues/210)). 자가신고 생년월일, `minimum_age_14` 증거, PG 결제 인증은 성인인증을 대체하지 않는다.
- `S` 로그아웃·계정 삭제·동의 철회.

### 5.2 IP 허브 (P0)

- `M` IP 목록/상세를 **비로그인 공개** 브라우징.
- `M` IP 상세: 배너·태그라인·시놉시스·지표(팬/굿즈/카드/이벤트), 탭(굿즈·카드·팝업·커뮤니티).
- `M` **무료 팔로우**(관심 IP 등록) → 홈/피드/알림 개인화.
- `S` 알림 설정(IP별 드롭·이벤트 알림 on/off).
- 버티컬: 캐릭터 IP · 게임 · 애니메이션 (버티컬별 액센트 유지).

### 5.3 굿즈 커머스 (P1) — 척추

- `M` 굿즈 목록(타입·IP·정렬·재고 상태), 굿즈 상세 — 갤러리·설명·**고시정보**(전자상거래 상품정보제공고시)·배송 안내를 포함한다.
- `M` 장바구니: 담기/수량/삭제, 비로그인 시 로컬 → 로그인 시 병합.
- `M` 체크아웃: 배송지 확인 → `pending` 주문 생성·재고 선점 → **토스페이먼츠 결제** → 웹훅 주문 확정.
- `M` **배송비**는 주문 시점 값을 스냅샷으로 고정한다. 기본 정액 + 일정 금액 이상 무료 임계.
- `M` 재고는 `pending` 주문 생성 시 원자적으로 선점하고, 결제 실패·만료 시 복원한다(품절 동시성).
- `M` 운영자는 현재 실재고를 확인하고 사유·멱등키·예상 수량을 포함한 audited RPC로만 입고·보정한다. 수량 0은 공개 화면에서 자동 품절로 파생하되 수동 판매중지는 보존한다. 외부 창고를 함께 쓰는 경우 이 수량은 **할당 재고**를 가리킨다([ADR-0005](./adr/0005-icons-allocated-inventory.md)).
- `M` 주문 내역·상태(결제완료/배송중/완료/취소).
- `M` **운송장** 등록·조회 — 운영자가 택배사·운송장번호를 남기고 구매자가 배송을 추적한다.
- `M` **청약철회/환불**(전자상거래법) 처리 흐름 — 배송 전 취소와 **배송 후 청약철회**를 모두 포함한다.
- `M` **주문 확인·배송 시작 이메일** — 계약내용에 관한 서면(전자문서) 교부 경로. [launch-readiness-plan §6](./launch-readiness-plan.md)의 "알림은 v1에서 인앱만" 가정에 대한 예외다.
- `S` 한정·신상·예약·품절 배지, 한정 수량 표시.

### 5.4 무료 카드 리워드 & 바인더 (P2)

> 유료 가챠·충전금 모델은 폐기됐다([ADR-0003](./adr/0003-free-reward-pivot.md), [ADR-0001](./adr/0001-paid-digital-gacha.md) superseded). 카드 리워드의 현재 계약은 [ADR-0004](./adr/0004-draw-ticket-card-packs.md)와 이 절이다.

- `M` **카드팩**: 조건에 맞는 굿즈 주문 확정은 특정 카드풀에 묶인 `draw_tickets`를 멱등 발급한다. 카드 자체를 유상 판매하거나 충전금·지갑을 제공하지 않는다.
- `M` **개봉**: 사용자가 카드팩을 열 때 `open_draw_ticket`이 서버에서 카드를 결정·발급한다. 클라이언트 reveal은 결과를 바꾸지 못한다.
- `M` **참여형 게임**: 무료 플레이의 결과와 카드 발급은 `play_game`이 서버에서 결정한다. legacy `goods` variant는 읽기 전용이고 신규 실물 판매에 사용하지 않는다.
- `M` **바인더**: 보유/미보유 카드, 등급(N·R·SR·SSR·HOLO), 도감 진행률, IP/등급 필터를 제공한다.
- `M` 카드풀 확률·운영 기간·정책·발급·개봉·회수는 감사 가능한 서버 원장으로 관리한다.
- 법률·PG 적용 여부를 "무료"라는 명칭만으로 단정하지 않는다. 상품가·발급 조건·표현·실제 운영에 대한 최신 검토 증거를 활성화 조건으로 둔다.

### 5.5 팝업/티케팅 (P3)

- `M` 이벤트 목록/상세(모드: 온/오프라인, 상태: 예매중/예정/진행중, 일시·장소).
- `M` **티켓 회차/종류·수량** 정의(운영자), **잔여 수량** 표시.
- `M` 예매: 수량 선택 → **재고 원자적 차감** → 토스페이먼츠 결제 → **전자티켓(QR) 발급**.
- `M` 입장: QR **검표**(중복 사용 방지), 사용/미사용 상태.
- `M` **예매 취소·환불**: 이벤트 시작 전 미사용 예매 전체만 취소하고, v1 수수료 0원으로 결제 금액 전액을 환불한다. 일정 미정·시작 이후·사용 티켓 포함 예매는 셀프 취소하지 않는다.
- `S` 1인 구매 한도, 예매 오픈 시각, 대기열(고동시성 회차).

### 5.6 커뮤니티 (P0)

- `M` 글 **작성**(텍스트 + 이미지 업로드), 태그, IP 연결.
- `M` 작성자는 visible 포스트의 텍스트·태그·IP 연결을 수정할 수 있다. 기존 이미지는 유지하고 hidden 포스트는 수정할 수 없다.
- `M` 댓글, 좋아요.
- `M` **신고 · 숨김 · 차단** (UGC 안전장치).
- `M` 운영자 모더레이션(`/admin`에서 신고 처리·포스트 또는 개별 댓글 비공개). 숨김 댓글은 공개 피드·preview·댓글 수에서 제외하고 운영자 화면에만 원문과 상태를 유지한다.
- `S` 트렌딩 태그, 팔로우 IP 기반 피드 개인화.
- 정책: 명예훼손·불법촬영물·저작권 침해 신고 대응 절차는 [`커뮤니티 운영 정책`](./community-moderation-policy.md)으로 분리한다. 운영 준비가 증명되기 전에는 post/comment 생성·수정 gate를 열지 않는다.

### 5.7 검색 (P0~)

- `M` 통합 검색: IP·굿즈·카드·포스트·태그.
- `M` 구현은 **Postgres pg_trgm/ILIKE**(한국어 부분일치). 결과 그룹핑.
- `S` 최근 검색어·인기 검색어.

### 5.8 운영 백오피스 `/admin` (각 도메인과 병행)

- `M` 역할 기반 접근(`admin`/`staff`). 일반 사용자 차단.
- `M` 카탈로그 관리: IP·굿즈·카드·카드풀 운영 기간·등급별 발급 확률·카드 풀 바인딩·이벤트·티켓 회차.
- `M` IP·굿즈·카드·이벤트는 hard delete하지 않고 참조 가드가 있는 audited RPC로 보관/복원한다. 공개 탐색과 신규 거래에서는 보관 항목을 제외하되 기존 주문·바인더·발급·팔로우·커뮤니티 이력의 FK와 조회 가능성은 유지한다.
- `M` 뽑기권 발급 정책: 구매 대상 IP와 선택 same-IP 굿즈, 독립 보상 카드풀, 최소 결제 금액, 발급 수량, 운영 기간·활성 상태를 관리한다. 한 주문에 조건이 맞는 정책은 모두 누적 적용한다.
- `M` 참여형 게임 운영: 카드 보상형 게임의 slug·제목·준비된 카드풀·같은 IP의 온라인 이벤트·KST 운영 기간·일일 한도를 audited RPC로 등록·수정하고 PII-free 플레이 집계를 확인한다. 신규 게임은 card variant·10개 구슬로 고정하고 등급 라인업은 카드풀 확률을 largest-remainder로 배분해 결정적으로 생성하되, 실제 결과는 `play_game`이 정한다. 게임 창 전체는 카드풀 창 안에 있어야 하며 연결된 카드풀·이벤트 변경도 이 계약을 깨뜨릴 수 없다. slug는 첫 플레이 전까지만 변경할 수 있고 플레이 후 type·카드풀·이벤트·config와 함께 잠긴다. 현재 시각이 운영 창에 포함되는 기존 카드 게임은 DB 현재 시각으로 종료하며 직접 DML은 허용하지 않는다. 기존 `goods` variant는 역사적 프로토타입으로 읽기 전용이며 실물 쿠지에 재사용하지 않는다.
- `M` 주문/티켓/환불 조회 및 처리.
- `M` 전체 사용자·특정 IP 팔로워 대상 인앱 공지의 수신자 수 미리보기·즉시 발송·감사 이력. 이메일·푸시·예약 발송은 제외한다.
- `M` 홈 큐레이션: 히어로·특집 IP·공지 배너의 제목·안전한 내부 링크·노출 순서·KST 기간·활성 상태를 관리한다. 공개 홈은 현재 활성인 큐레이션만 반영하며, 공지 배너 저장은 인앱 공지를 자동 발송하지 않는다.
- `M` 커뮤니티 신고 처리.
- `M` 회원 운영: 목록 이메일은 마스킹하고 명시적 상세에서만 전체 이메일·현재 동의 상태·최소 운영 집계를 조회한다. active staff는 user, active admin은 user/staff를 정지·해제할 수 있으며 본인과 admin 대상은 제외한다. 정지 계정은 새 작성·댓글·주문·예매·카드팩 개봉·게임 플레이와 staff 권한을 잃되, 기존 주문·환불·삭제·신고 같은 회복 경로는 유지한다. 내부 사유는 상세 RPC에만 노출하고 감사 로그에는 남기지 않는다.
- `M` **감사 로그**(audit_log): 민감 작업 기록.

---

## 6. 핵심 사용자 플로우

1. **첫 방문(비로그인)**: 홈 → IP 탐색 → 굿즈/카드 구경(공개). 액션 시 로그인 유도.
2. **온보딩**: 소셜/이메일 로그인 → 이메일·닉네임·생년월일·동의 입력 → 관심 IP 팔로우.
3. **비밀번호 재설정**: 로그인 → 재설정 메일 요청 → 요청한 브라우저에서 최신 링크 열기 → 새 비밀번호 저장 → 새 비밀번호로 로그인.
4. **굿즈 구매**: 굿즈 상세 → 장바구니 → 체크아웃 → 토스 결제 → 주문완료 → 내역.
5. **카드 리워드**: 굿즈 주문 또는 무료 참여형 게임 → 카드팩 발급/서버 결과 확정 → 개봉 연출 → 바인더 적립.
6. **티켓**: 이벤트 상세 → 회차/수량 선택 → 토스 결제 → 전자티켓(QR) → 현장 검표.
7. **커뮤니티**: 피드 → 글 작성(이미지)·수정 → 반응/댓글 → (문제 시) 신고/차단.

---

## 7. 규제 · 법무 요구사항

선택한 기능(실물 굿즈·티켓 결제, 무료 카드 리워드, UGC)과 별도 승인 트랙은 다음 검토 표면을 동반한다. 적용 여부를 문서만으로 단정하지 않고 제품·법무·공급사 evidence를 출시 전에 맞춘다.

| 항목 | 적용 | 요구사항 |
|---|---|---|
| **전자상거래법** | 굿즈·티켓·실물 쿠지 | 사업자/판매자 정보·약관·상품 고지·청약철회·환불·표시광고 준수, **통신판매업 신고** |
| **게임산업·사행성 관련 검토** | 카드팩·참여형 게임·실물 쿠지 | 명칭이나 무상 주장만으로 적용 제외를 단정하지 않고 실제 대가성·게임성·운영 구조를 법률 검토와 일치시킨다 |
| **개인정보보호법(PIPA)** | 전체 | 수집 항목 최소화·동의·보관/파기. 14+ 가입 기준과 기존 계정 처리의 상세 계약은 #188에서 승인한다 |
| **청소년보호·성인 콘텐츠** | 19+ 상품 | NICE `adult_19` 증거 없이 상세·구매·결과 미디어에 접근시키지 않는다. PG 인증은 연령보증이 아니다 |
| **저작권/라이선스** | IP·굿즈·아트워크 | 공식 라이선스 범위 내 사용, 침해 신고 대응 |

---

## 8. 비기능 요구사항

- **무결성**: 카드 발급·티켓 재고·주문·실물 경품 배정은 동시성에서도 **원자적**이어야 한다(중복 발급·이중 차감·초과 판매 금지). → Postgres RPC + 행 잠금([아키텍처](./ARCHITECTURE.md) 참조).
- **결제 신뢰성**: 결제 확정은 **웹훅 기반 + 멱등 처리**. 클라이언트 성공 신호만으로 주문을 확정하지 않는다.
- **보안**: 사용자 데이터는 Supabase **RLS**로 소유자 범위 격리. 카탈로그는 공개 읽기. 관리자 작업은 역할+감사 로그.
- **성능**: 공개 카탈로그는 캐시 가능(SSR/정적). LCP 목표 양호 수준, 이미지 최적화.
- **가용성/관측성**: 결제·카드 발급·티켓 트랜잭션 로깅, 실패 알림.
- **접근성/반응형**: 모바일 우선(MobNav), 데스크톱 Nav. 한국어 기본.
- **디자인 일관성**: "Holographic Midnight" 디자인 시스템(`app/globals.css`) 유지.

---

## 9. 출시 단계

범위는 v1로 동일하되, **위험도·의존성 순**으로 단계 출시한다.

| 단계 | 내용 | 트랜잭션 |
|---|---|---|
| **P0 — 기반** | Supabase Auth·DB·RLS, 카탈로그 브라우징(IP/굿즈/카드/이벤트), IP 팔로우, 커뮤니티(작성형), 검색, `/admin` 골격 | 비결제 |
| **P1 — 커머스** | 장바구니·체크아웃·**토스페이먼츠**·주문·환불, 재고 차감 | 결제 #1 |
| **P2 — 카드 리워드** | 카드팩 발급·서버 개봉 RNG, 참여형 게임, 바인더 적립 | 비결제 리워드 |
| **P3 — 티케팅** | 회차/재고, 예매·결제, 전자티켓(QR)·검표, 예매 환불 | 결제 #3 |

각 단계는 독립 출시 가능하도록 설계한다. 별도 19+ 실물 쿠지 트랙은 이 표에 포함하지 않고 #212·#213의 선행 증거와 gate를 따른다.

---

## 10. 성공 지표 (초안)

- 활성화: 가입→온보딩 완료율, 관심 IP 1개 이상 팔로우 비율.
- 커머스: 장바구니→결제 전환율, 결제 성공률.
- 카드 리워드: 카드팩 발급→개봉률, 참여형 게임 완료율, 바인더 도감 진행률.
- 티켓: 예매 성공률, 검표 정상률.
- 커뮤니티: 작성/반응 비율, 신고 처리 시간.

---

## 11. 미해결 사항 / 가정

- **14+ 가입 기준**: 공통 `AgeAssurance` 계약, 기존 계정 dry-run과 법정 문서는 #188 acceptance evidence가 필요하다. 현재 자가신고는 완료 증거가 아니다.
- **19+ 성인인증**: NICE 계약·callback·최소 데이터는 #209, 실제 상품 gate는 #210에서 별도 추적한다.
- 카드의 **디지털 자산 성격**(소유 이전 가능 여부)은 v1에서 비이전(계정 귀속) 가정. 교환은 v2.
- 현재 runtime은 Toss 굿즈·티켓 거래를 유지하고, 신규 Korpay checkout과 provider-neutral 원장은 별도 결제 티켓에서 전환한다. 이 문서 정리는 provider 활성화를 의미하지 않는다.
- **BM 확정(2026-07-03)**: 유료 가챠를 폐기하고 **무료 리워드 모델**로 피벗했다([`docs/adr/0003-free-reward-pivot.md`](./adr/0003-free-reward-pivot.md), 0001 superseded). §5.4의 유상 가챠·충전금·확률 공시·전자금융 항목은 이 결정으로 대체된다.
- 관련 산출물: 도메인 용어는 `CONTEXT.md`, 무료 카드 리워드는 [ADR-0003](./adr/0003-free-reward-pivot.md)·[ADR-0004](./adr/0004-draw-ticket-card-packs.md), 19+ 유한 실물 쿠지는 #212·#213이 각각 정본이다. [ADR-0002](./adr/0002-cross-platform-popup-game-miniapps.md)는 superseded 이력이며 현 로드맵의 구현 근거가 아니다.
