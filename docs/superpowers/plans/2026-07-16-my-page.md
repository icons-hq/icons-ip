# #103 마이페이지 통합 진입점 구현 계획

> 대상: `ps/feat/my-page` worktree. 각 동작은 실패 테스트를 먼저 추가하고 최소 구현으로 통과시킨다.

## 1. 라우트 계약과 보호 화면

파일:

- 수정: `lib/routes.test.ts`, `lib/routes.ts`
- 생성: `lib/profile-avatar.server.test.ts`, `lib/profile-avatar.server.ts`
- 수정: `app/settings/page.test.tsx`, `app/settings/page.tsx`
- 생성: `app/my/page.test.tsx`, `app/my/page.tsx`

순서:

1. `my → /my` 매핑·active 테스트를 실패시킨다.
2. 비로그인·온보딩 미완료 redirect, signed URL 성공·실패 props 테스트를 실패시킨다.
3. settings의 signed-avatar 읽기를 공용 server helper로 옮기고 기존 동작을 보존한다.
4. 기존 Auth/Profile helper를 조합하는 Server Component page로 통과시킨다.

## 2. 마이 허브 화면

파일:

- 생성: `components/screens/MyPage.test.tsx`, `components/screens/MyPage.tsx`
- 수정: `app/globals.css`, `components/shell/Atmos.tsx`

순서:

1. 닉네임·아바타/fallback과 5개 실제 링크의 정적 markup 테스트를 실패시킨다.
2. 의미론적 section/list/link 구조로 화면을 구현한다.
3. Holographic Midnight 전용 클래스와 `/my` atmosphere, 920/620px 반응형·reduced-motion·focus-visible 규칙을 추가한다.

## 3. 로그인 상태 공유와 내비게이션

파일:

- 생성: `components/shell/AuthPresenceProvider.tsx`
- 생성: `components/shell/AccountEntrypoints.test.tsx`
- 수정: `app/layout.tsx`, `components/shell/AuthButton.tsx`, `components/shell/MobNav.tsx`

순서:

1. 기존 `AuthButton`의 초기 사용자 조회·auth change 구독·해제 로직을 표시 전용 tri-state `AuthPresenceProvider`로 이동한다.
2. 로그인 데스크톱 `마이` 링크·로그아웃, 비로그인 로그인 CTA 테스트를 실패시킨 뒤 통과시킨다.
3. 로그인 모바일 `마이`/비로그인 `장바구니`, active·장바구니 배지 테스트를 실패시킨 뒤 통과시킨다.

## 4. 문서와 회귀 검증

파일:

- 수정: `DESIGN.md`, `docs/ARCHITECTURE.md`, `docs/launch-readiness-plan.md`

순서:

1. 셸/표면/라우트 as-built와 #103 완료 상태를 현재 코드에 맞춘다.
2. 관련 테스트 → 전체 `npm test` → `npm run lint` → `npm run build` → `git diff --check`를 실행한다.
3. 로컬 브라우저에서 비로그인 gate, 로그인 desktop/mobile 허브와 내비를 확인한다.
4. 독립 코드·UI·보안 리뷰 후 PR, CI, squash merge, production route/E2E, issue·Project Done까지 닫는다.
